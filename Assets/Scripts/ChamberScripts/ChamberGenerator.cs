using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChamberGenerator : MonoBehaviour
{
    //public void GenerateNeghiborChambersIfNecessary(Chamber[] listOfAnticipatedChambers)
    //{
    //    for (int anticipatedChamber = 0; anticipatedChamber < listOfAnticipatedChambers; i++)
    //    {
    //        if (anticipatedChamber[1].neighborsDescribed == False)
    //        {
    //            self.storyTeller.DescribeNeighborChambers(anticipatedChamber);
    //            anticipatedChamber[1].neighborsDescribed == True;
    //        }
    //    }

    //}
            

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
